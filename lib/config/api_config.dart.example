// API Configuration Template
// 
// SETUP INSTRUCTIONS:
// 1. Copy this file to api_config.dart: 
//    cp lib/config/api_config.dart.example lib/config/api_config.dart
// 2. Replace placeholder API keys below with your actual keys
// 3. NEVER commit api_config.dart - it's in .gitignore
//
// Get API Keys:
// - Google Gemini (FREE): https://aistudio.google.com/apikey
// - OpenAI (Paid): https://platform.openai.com/api-keys

class ApiConfig {
  // ============================================
  // AI CONFIGURATION (Choose One)
  // ============================================
  
  // OPTION 1: Google Gemini (FREE - Recommended!)
  // Get free API key: https://aistudio.google.com/apikey
  static const String geminiApiKey = 'YOUR_GEMINI_API_KEY_HERE';
  static const bool useGemini = false;  // Set to true when you add your Gemini key
  
  // OPTION 2: OpenAI (Paid - Requires billing)
  // Get API key: https://platform.openai.com/api-keys
  static const String openAiApiKey = 'YOUR_OPENAI_API_KEY_HERE';
  static const bool useOpenAI = false;  // Set to true when you add your OpenAI key
  
  // API Endpoints
  static const String geminiBaseUrl = 'https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent';
  static const String geminiModel = 'gemini-2.5-flash';  // Fast & Free!
  static const String openAiBaseUrl = 'https://api.openai.com/v1';
  static const String openAiModel = 'gpt-3.5-turbo'; // Cheaper model for testing ($0.002 per recipe vs $0.015 for gpt-4o)
  // To use GPT-4o again: Change to 'gpt-4o'
  
  // API Settings
  static const int maxTokens = 2000;
  static const double temperature = 0.7;
}
